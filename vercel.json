{
  "installCommand": "npm --prefix server install && npm --prefix app install",
  "buildCommand": "npm --prefix app run vercel-build",
  "outputDirectory": "app/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
